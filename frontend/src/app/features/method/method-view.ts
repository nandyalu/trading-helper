import { Component } from '@angular/core';

/**
 * How the experiment is run, and what is wrong with it.
 *
 * Written for the reader who wants to know whether any of this is worth
 * believing. That reader is not convinced by results; they are convinced by
 * the limitations being stated before they have to find them.
 *
 * **The limitations section is the point of the page.** Publishing what the
 * design cannot answer is what separates an experiment from a product demo,
 * and it is the section to extend first whenever something new is learned.
 */
@Component({
  selector: 'app-method-view',
  templateUrl: './method-view.html',
})
export class MethodView {}
